version: '2'
services:
  sphinx:
    build:
      context: .
    command: "searchd --console --config /opt/sphinx.conf"
    ports:
    - "36307:36307"
    volumes:
    - ./data:/opt/sphinx/index
    logging:
      driver: syslog
      options:
        tag: sphinx
    hostname: sphinx-docker
